{
  "name": "mspi-tools",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "apps/*/frontend",
    "apps/*/backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build:shared": "npm exec --workspace=@mspi/shared-db -- tsc && npm exec --workspace=@mspi/shared-auth -- tsc && echo 'SHARED_BUILD_COMPLETE'",
    "build:tool-backends": "npm exec --workspace=@mspi/pcount-backend -- tsc && npm exec --workspace=@mspi/rfpu-backend -- tsc && echo 'TOOL_BACKENDS_BUILD_COMPLETE'",
    "build:frontends": "npm exec --workspace=@mspi/pcount-frontend -- tsc -b && npm exec --workspace=@mspi/pcount-frontend -- vite build && npm exec --workspace=@mspi/rfpu-frontend -- tsc -b && npm exec --workspace=@mspi/rfpu-frontend -- vite build && npm exec --workspace=@mspi/launcher-frontend -- tsc -b && npm exec --workspace=@mspi/launcher-frontend -- vite build && echo 'FRONTENDS_BUILD_COMPLETE'",
    "build:gateway": "npx esbuild apps/launcher/backend/src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=apps/launcher/backend/dist/index.js --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\" 2>&1 && echo 'GATEWAY_BUILD_COMPLETE'",
    "build:assemble": "mkdir -p apps/launcher/backend/dist/public/pcount && rm -rf apps/launcher/backend/dist/public/pcount/* && cp -r apps/pcount/frontend/dist/. apps/launcher/backend/dist/public/pcount/ && mkdir -p apps/launcher/backend/dist/public/rfpu && rm -rf apps/launcher/backend/dist/public/rfpu/* && cp -r apps/rfpu/frontend/dist/. apps/launcher/backend/dist/public/rfpu/ && cp -r apps/launcher/frontend/dist/. apps/launcher/backend/dist/public/ && ls -la apps/launcher/backend/dist/public/ && ls -la apps/launcher/backend/dist/public/*/",
    "build": "npm run build:shared && npm run build:tool-backends && npm run build:frontends && npm run build:gateway && npm run build:assemble",
    "seed": "npm run seed -w @mspi/launcher-backend",
    "db:push": "npm run db:push -w @mspi/launcher-backend",
    "db:generate": "npm run db:generate -w @mspi/launcher-backend",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.28.1",
    "turbo": "^2.1.0"
  },
  "dependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.21"
  },
  "packageManager": "npm@10.9.3"
}
