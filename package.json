{
  "name": "mspi-tools",
  "private": true,
  "main": "backend/dist/index.js",
  "scripts": {
    "dev": "node scripts/prepare-dev.mjs && cd backend && npm install --no-audit --no-fund && cd ../frontend && npm install --no-audit --no-fund && cd .. && concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "build": "npm ci && cd backend && npm ci && npm run build && cd ../frontend && npm ci && npm run build && cd .. && node scripts/publish-frontend.mjs",
    "start": "node backend/dist/index.js",
    "seed": "cd backend && npm run seed",
    "db:push": "cd backend && npm run db:push",
    "db:generate": "cd backend && npm run db:generate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.5.0"
  },
  "packageManager": "npm@10.9.3"
}
